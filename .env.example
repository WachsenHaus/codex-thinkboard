# The local Thinkboard canvas stays on loopback by default.
THINKBOARD_WEB_HOST=127.0.0.1
THINKBOARD_WEB_PORT=43127

# Optional: override the default ~/.codex/thinkboard data directory.
# THINKBOARD_DATA_DIR=/absolute/local/path
